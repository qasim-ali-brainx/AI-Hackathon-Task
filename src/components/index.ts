/**
 * Shared React components for popup and options pages.
 *
 * Future components:
 * - ToggleSwitch — reusable Focus Mode toggle control
 * - WhitelistEditor — domain whitelist management UI
 * - SchedulePicker — time-based Focus Mode scheduling
 * - SessionReportCard — summary card for blocked notification stats
 */
