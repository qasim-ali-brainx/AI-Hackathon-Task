/**
 * Shared utility functions.
 *
 * Future utilities:
 * - formatDuration — human-readable session length
 * - extractDomain — normalize URLs to domain strings for analytics
 * - generateId — create unique IDs for sessions and blocked events
 */
